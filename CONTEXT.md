# Habiquest

한 사람이 자기 습관을 **과학적 시스템**으로 다루도록 돕는 앱. 설계(가설) → 실행 →
기록(데이터) → 회고(가설 수정)의 닫힌 루프가 이 제품의 전부다.

## Language

### 기록의 하루

**미스 (miss)**:
어떤 하루가 실패로 **집계되는 성질**. 하루의 상태가 아니라 상태에서 파생되는 판정이다
(`isMissDay`). 연속·미스 카운트·성공률이 이것을 읽는다.
_Avoid_: 실패(상태 이름으로 쓸 때), fail

**missed**:
지난 날인데 기록 행이 하나도 없는 **하루 상태**. 미스이지만 **이유를 담지 않는다** —
그래서 어떤 성분(cue/floor/identity/load)에도 귀속되지 않는다. 뒤늦게 기록을 덧붙이면
다른 상태로 다시 분류되고 그 날을 통과하는 모든 수치가 회복된다.
_Avoid_: blank, unknown, 빈 날

**skip**:
사용자가 **사유와 함께** 직접 "안 함"으로 찍은 하루 상태. `exception` 사유를 뺀
나머지는 미스이며, **성분 귀속을 만드는 유일한 상태**다.
_Avoid_: 건너뜀만으로 지칭하기(사유가 붙는다는 점이 핵심)

**partial**:
실제 활동을 기록했지만 그 날의 합이 최소량에 못 미친 하루 상태. **미스가 아니다.**
_Avoid_: 부분 실패, 미달 실패

**pending**:
오늘이고 아직 기록이 없는 하루 상태. 하루가 끝나지 않았으므로 미스가 아니다.

### 습관의 설계

**최소량 (floor)**:
못 할 수가 없는 하한. 이걸 넘긴 하루가 "했다"이고, 연속과 기본 XP의 기준이다.
_Avoid_: 목표, 최소 목표, minimum goal

**목표 (target)**:
최소량보다 **엄격히 큰** 선택적 상향 목표. 넘기면 보너스지만 요구되지는 않는다.

**성분 (component)**:
진단이 고장을 귀속시키는 네 축 — `cue` / `floor` / `identity` / `load`. 진단은
성분을 지목하고, 처방은 별도의 결정론적 규칙이 만든다.
